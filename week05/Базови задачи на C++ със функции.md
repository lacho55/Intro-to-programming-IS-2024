# Базови задачи на C++ със функции
```cpp
 Задача 1: Функция за намиране на факториел на число
Напишете функция `int factorial(int n)`, която приема цяло число `n` и връща факториела му.

**Примерен вход:**

int result = factorial(5);
Очакван изход:

Copy code
120
Задача 2: Функция за проверка на просто число
Напишете функция bool isPrime(int n), която приема цяло число n и връща true, ако числото е просто, и false в противен случай.

Примерен вход:

cpp
Copy code
bool result = isPrime(7);
Очакван изход:

arduino
Copy code
true
Задача 3: Функция за намиране на най-голям общ делител (НОД)
Напишете функция int gcd(int a, int b), която приема две цели числа a и b и връща най-големия им общ делител.

Примерен вход:

cpp
Copy code
int result = gcd(24, 36);
Очакван изход:

Copy code
12
Задача 4: Функция за пресмятане на повдигане на степен
Напишете функция double power(double base, int exponent), която приема основа (base) и степен (exponent) и връща резултата от base^exponent.

Примерен вход:

cpp
Copy code
double result = power(2, 3);
Очакван изход:

Copy code
8
Задача 5: Функция за обръщане на число
Напишете функция int reverseNumber(int n), която приема цяло число n и връща числото, обърнато назад.

Примерен вход:

cpp
Copy code
int result = reverseNumber(1234);
Очакван изход:

yaml
Copy code
4321
Задача 6: Функция за намиране на сумата на цифрите на число
Напишете функция int sumOfDigits(int n), която приема цяло число n и връща сумата на неговите цифри.

Примерен вход:

cpp
Copy code
int result = sumOfDigits(123);
Очакван изход:

Copy code
6
Задача 7: Функция за проверка дали число е палиндром
Напишете функция bool isPalindrome(int n), която проверява дали дадено число е палиндром (чете се еднакво напред и назад).

Примерен вход:

cpp
Copy code
bool result = isPalindrome(121);
Очакван изход:

arduino
Copy code
true
Задача 8: Функция за сортиране на масив
Напишете функция void sortArray(int arr[], int n), която приема масив от цели числа и броя на елементите и го сортира във възходящ ред.

Примерен вход:

cpp
Copy code
int arr[] = {5, 2, 8, 1, 3};
sortArray(arr, 5);
Очакван изход:

Copy code
1, 2, 3, 5, 8
Задача 9: Функция за намиране на средно аритметично на масив
Напишете функция double average(int arr[], int n), която приема масив от цели числа и броя на елементите и връща средното им аритметично.

Примерен вход:

cpp
Copy code
int arr[] = {2, 4, 6, 8, 10};
double result = average(arr, 5);
Очакван изход:

Copy code
6
Задача 10: Функция за намиране на минимален и максимален елемент в масив
Напишете функция void minMax(int arr[], int n, int &min, int &max), която приема масив от цели числа, броя на елементите и референции за минималния и максималния елемент. Функцията намира минималната и максималната стойност в масива.

Примерен вход:

cpp
Copy code
int arr[] = {3, 5, 1, 9, 2};
int min, max;
minMax(arr, 5, min, max);
Очакван изход:

arduino
Copy code
min = 1, max = 9
Задача 11: Функция за преобразуване на градуси по Целзий в Фаренхайт
Напишете функция double celsiusToFahrenheit(double celsius), която приема температура в градуси по Целзий и връща еквивалентната температура по Фаренхайт.

Примерен вход:

cpp
Copy code
double result = celsiusToFahrenheit(25);
Очакван изход:

Copy code
77
Задача 12: Функция за проверка дали година е високосна
Напишете функция bool isLeapYear(int year), която приема година и връща true, ако годината е високосна, и false в противен случай.

Примерен вход:

cpp
Copy code
bool result = isLeapYear(2020);
Очакван изход:

arduino
Copy code
true
Задача 13: Функция за намиране на разстояние между две точки в 2D пространство
Напишете функция double distance(double x1, double y1, double x2, double y2), която приема координатите на две точки в равнината и връща разстоянието между тях.

Примерен вход:

cpp
Copy code
double result = distance(1, 2, 4, 6);
Очакван изход:

Copy code
5
Задача 14: Функция за генериране на първите n числа на Фибоначи
Напишете функция void fibonacci(int n), която приема цяло число n и извежда първите n числа от редицата на Фибоначи.

Примерен вход:

cpp
Copy code
fibonacci(5);
Очакван изход:

Copy code
0, 1, 1, 2, 3
Задача 15: Функция за преобразуване на низ към главни букви
Напишете функция void toUpperCase(std::string &str), която приема референция към низ и преобразува всички букви към главни.

Примерен вход:

cpp
Copy code
std::string str = "hello";
toUpperCase(str);
Очакван изход:

Copy code
HELLO
yaml
Copy code
---
